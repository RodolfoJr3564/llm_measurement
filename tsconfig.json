{
  "compilerOptions": {
    /* Language and Environment */
    "lib": ["ES2023"],                                   /* Inclui todas as definições de tipo disponíveis para a versão do ECMAScript. */
    "target": "ES2022",                                  /* Aumenta o nível do ECMAScript para compatibilidade com mais recursos modernos. */
    "module": "node16",                                  /* Usa módulos ESNext para compatibilidade com ES6+ features, ideal para o tsx. */
    
    /* Modules */
    "esModuleInterop": true,                             /* Suporte adicional para interoperabilidade entre ESModules e CommonJS. */
    "allowSyntheticDefaultImports": true,                /* Permite a importação de módulos sem exportação default de forma mais simples. */
    
    /* Strict Type-Checking Options */
    "strict": true,                                      /* Habilita todas as verificações estritas, o que é uma boa prática. */
    "noImplicitAny": true,                               /* Garante que todas as variáveis tenham tipos explícitos. */
    "strictNullChecks": true,                            /* Impede atribuição de null ou undefined a variáveis não explicitamente permitidas. */
    
    /* Source Maps */
    "sourceMap": true,                                   /* Gera mapas de origem para facilitar a depuração. */
    
    /* Completeness */
    "skipLibCheck": true                                 /* Ignora a verificação de tipos em arquivos de definição de biblioteca para maior velocidade de compilação. */
  },
  "include": ["src/**/*"],                               /* Inclui todos os arquivos na pasta src. */
  "exclude": ["node_modules", "dist"]                    /* Exclui a pasta node_modules e dist. */
}